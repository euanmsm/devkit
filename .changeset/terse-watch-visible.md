---
'@euanmsm/terse': patch
---

Tell the user, not only the agent. `terse-watch` now returns a one-line
`systemMessage` alongside the findings it hands the agent, so the terminal shows
that it fired.
